# Documentation only; Flutter uses --dart-define, not dotenv at runtime.
# The provider URL is fixed in lib/core/portal_config.dart.
# SERVER_URL is no longer accepted. HTTPS migration requires an app update.
# Debug builds only. HTTP requires an explicit in-app acknowledgement too.
ALLOW_HTTP=false
